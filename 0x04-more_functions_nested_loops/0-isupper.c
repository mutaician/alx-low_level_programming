#include "main"

/**
 * _isupper - prints upper case
 * @c: any character
 *
 * Return: 1 if c is uppercase
 *0 if otherwise
 */
int _isupper(int c)
{
	if (c >= 65 && <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
